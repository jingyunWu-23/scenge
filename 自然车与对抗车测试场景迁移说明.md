# 自然车与对抗车测试场景迁移说明

本文档说明如何把本项目的两类 CARLA 测试场景迁移到其他实验中使用：

1. 纯自然车测试场景：只测试主车与自然车，主车策略冻结，自然车由车辆控制器或 TrafficManager 控制。
2. 冻结对抗车测试场景：主车策略冻结，对抗车加载我们已训练好的 MAPPO 对抗车模型，测试主车在 10 个固定对抗车场景下的表现。

两类测试都使用同一条直线路线、同一套低维观测和同一套指标日志，便于不同主车方法之间做横向对比。

## 依赖文件

迁移时至少需要保留以下文件和目录结构：

```text
carla_evolution/
  agents/
    ego_ppo.py
    mappo.py
  configs/
    carla_0915.yaml
  envs/
    action_adapter.py
    env.py
    factory.py
    metrics.py
    observation.py
    reward.py
    scenario_manager.py
    vehicle_state.py
  scripts/
    evaluate_finetuned_ego_against_adv.py
    finetune_ego_enhanced_poet.py
    run_ego_checkpoint_sweep_eval.py
    run_ego_natural_seed_eval.py
  training/
    train.py
  results/
    joint_Jul_01_14_59_59/
      joint_round_log.csv
      models/
        adv/
```

如果要直接加载本项目 EgoPPO 主车，还需要 `MARL1/ego` 相关模块和 PyTorch 环境。如果其他实验使用自己的主车策略，可以不复用 `agents/ego_ppo.py`，但需要实现一个能输出本项目离散动作的 adapter。

## 主车动作接口

当前环境接收离散动作，定义在 `envs/action_adapter.py`：

```text
0: LANE_LEFT
1: KEEP_LANE
2: LANE_RIGHT
3: ACCELERATE
4: DECELERATE
```

其他方法接入时，建议实现一个主车 adapter，输入当前主车观测，输出上述 0 到 4 的动作编号。然后在测试循环里替换：

```python
ego_action, _, _ = ego.select_action(ego_state, deterministic=True)
```

为自己的策略推理：

```python
ego_action = your_policy_adapter.act(ego_state)
```

只要输出动作编号一致，后续场景、车辆控制、奖励和指标统计都可以复用。

## 场景初始化规则

两类测试都通过 `scripts/finetune_ego_enhanced_poet.py` 中的场景模板生成环境配置，核心函数是：

```python
make_env_spec(...)
env_config_from_spec(...)
```

默认使用的模板是：

```text
slow_front_left_escape
```

该模板对应直道场景，路线长度为 `200m`，最大时间步通常设为 `200`，每步 `0.05s`，所以每个 episode 的仿真时长为 `10s`。

为了保证测试覆盖更广，建议开启车辆初始化随机化：

```bash
--randomize-scenarios
--relative-offset-jitter 10.0
--randomize-lane-offsets
--randomize-lane-offset-min 0
--randomize-lane-offset-max 2
```

含义：

```text
relative-offset-jitter 10.0: 车辆纵向相对位置在基础 offset 上做 ±10m 扰动
randomize-lane-offsets: 随机车辆初始车道偏移
lane-offset-min/max: 车道偏移范围
```

如果只想随机纵向位置，不随机车道，可以去掉 `--randomize-lane-offsets`。

## 纯自然车测试

纯自然车测试入口：

```text
scripts/run_ego_natural_seed_eval.py
```

推荐调用方式：

```bash
cd /path/to/carla_evolution

/path/to/python scripts/run_ego_natural_seed_eval.py \
  --backend carla \
  --config configs/carla_0915.yaml \
  --ego-checkpoint /path/to/ego/checkpoint.pt \
  --checkpoint-interval 999999999 \
  --seed-count 10 \
  --eval-seed-base 101 \
  --num-natural 3 \
  --max-steps 200 \
  --hdv-control-mode fixed \
  --hdv-action keep_lane \
  --randomize-scenarios \
  --relative-offset-jitter 10.0 \
  --randomize-lane-offsets \
  --randomize-lane-offset-min 0 \
  --randomize-lane-offset-max 2 \
  --carla-rpc-timeout 300 \
  --output-dir results/ego_natural_seed_eval/your_method_natural_eval \
  --rerun-existing
```

其中：

```text
--hdv-control-mode fixed
```

表示自然车由本项目车辆控制器控制，即离散动作 `keep_lane` 会经过 `ActionAdapter` 和 `ScenarioManager._to_vehicle_control()` 转成 CARLA `VehicleControl`。

如果想复现之前 TrafficManager 自然车测试，可以改成：

```bash
--hdv-control-mode traffic_manager \
--traffic-manager-port 8000 \
--hdv-tm-speed-difference 0.0 \
--hdv-tm-distance-to-leading-vehicle 8.0
```

纯自然车测试输出包括：

```text
episode_log.csv
overall_checkpoint_comparison.csv
scenario_mean_variance.csv
model_comparison_mean_plus_variance.csv
scenario_family_comparison_mean_plus_variance.csv
figures/*.png
```

## 冻结对抗车测试

冻结对抗车测试入口：

```text
scripts/evaluate_finetuned_ego_against_adv.py
```

批量测试多个主车 checkpoint 时使用：

```text
scripts/run_ego_checkpoint_sweep_eval.py
```

我们之前使用的 10 个对抗车场景来自：

```text
results/joint_Jul_01_14_59_59/models/adv
results/joint_Jul_01_14_59_59/joint_round_log.csv
```

对应对抗车 checkpoint step 为：

```text
8950
9730
10590
12060
12200
13380
14930
15530
16500
17410
```

这些 checkpoint 是从联合训练后 60 轮中分散抽样得到的。调用时不需要手工列出这 10 个 step，只要传入同样的抽样参数即可复现：

```bash
--adv-model-dir results/joint_Jul_01_14_59_59/models/adv \
--joint-round-log results/joint_Jul_01_14_59_59/joint_round_log.csv \
--adv-round-last 60 \
--adv-sample-count 10 \
--eval-episodes-per-adv 10 \
--eval-seed-base 101 \
--eval-seed-stride 100000
```

单个主车模型测试命令：

```bash
cd /path/to/carla_evolution

/path/to/python scripts/evaluate_finetuned_ego_against_adv.py \
  --config configs/carla_0915.yaml \
  --ego-checkpoint /path/to/ego/checkpoint.pt \
  --adv-model-dir results/joint_Jul_01_14_59_59/models/adv \
  --joint-round-log results/joint_Jul_01_14_59_59/joint_round_log.csv \
  --adv-round-last 60 \
  --adv-sample-count 10 \
  --eval-episodes-per-adv 10 \
  --eval-seed-base 101 \
  --eval-seed-stride 100000 \
  --num-adv 3 \
  --num-natural 0 \
  --max-steps 200 \
  --randomize-scenarios \
  --relative-offset-jitter 10.0 \
  --randomize-lane-offsets \
  --randomize-lane-offset-min 0 \
  --randomize-lane-offset-max 2 \
  --carla-rpc-timeout 300 \
  --output-dir results/ego_adv_frozen_eval/your_method_adv_eval
```

批量测试多个主车模型命令：

```bash
/path/to/python scripts/run_ego_checkpoint_sweep_eval.py \
  --config configs/carla_0915.yaml \
  --ego-dir /path/to/ego/checkpoint_dir \
  --checkpoint-interval 100 \
  --include-final \
  --label-with-source \
  --adv-model-dir results/joint_Jul_01_14_59_59/models/adv \
  --joint-round-log results/joint_Jul_01_14_59_59/joint_round_log.csv \
  --adv-round-last 60 \
  --adv-sample-count 10 \
  --eval-episodes-per-adv 10 \
  --eval-seed-base 101 \
  --eval-seed-stride 100000 \
  --num-adv 3 \
  --num-natural 0 \
  --max-steps 200 \
  --randomize-scenarios \
  --relative-offset-jitter 10.0 \
  --randomize-lane-offsets \
  --randomize-lane-offset-min 0 \
  --randomize-lane-offset-max 2 \
  --carla-rpc-timeout 300 \
  --cleanup-destroy-mode sequential \
  --output-dir results/ego_adv_frozen_eval/your_method_adv_sweep \
  --rerun-existing
```

如果主车模型不在同一个目录，可以重复传：

```bash
--ego-checkpoint /path/to/model_a.pt \
--ego-checkpoint /path/to/model_b.pt
```

`--label-with-source` 用于避免不同实验目录下相同 checkpoint 编号被合并。

## 使用我们已经封装好的两类批量脚本

如果只是在当前项目内复现实验，可以直接运行：

```bash
bash scripts/run_ego_natural_first_last_finetunes_eval.sh
```

该脚本会测试：

```text
训练结束主车 checkpoint-67210.pt
6 个微调超参实验目录中的第一个和最后一个主车模型
```

纯自然车输出目录：

```text
results/ego_natural_seed_eval/ego67210_plus_6finetunes_first_last_tm_hdv3_random10
```

对抗车批量测试：

```bash
bash scripts/run_ego_adv_first_last_finetunes_eval.sh --cuda-visible-devices 0
```

对抗车输出目录：

```text
results/ego_adv_frozen_eval/ego67210_plus_6finetunes_first_last_adv10_random_pos_lane
```

## 指标说明

主要比较指标包括：

```text
route_completion: 道路完成率
ego_route_distance_m: 主车行驶距离
mean_speed: 主车平均速度
combined_efficiency: 通行效率
episode_reward: 主车奖励
crash / ego_crash: 主车碰撞
ego_lane_change_success_flag: 是否出现成功变道
ego_overtake_success_flag: 是否出现超车成功
front_slow_flag: 是否出现前车慢速
safe_lane_change_opportunity_flag: 是否存在低风险可换道机会
```

表格中的 `均值 ± 方差` 使用同一模型在同一类场景下所有 episode 的结果计算，方差为总体方差 `ddof=0`。

## CARLA 运行要求

运行前需要先启动 CARLA server，并保证 Python API 版本和 CARLA server 版本一致。常见启动方式：

```bash
./CarlaUE4.sh -quality-level=Low -carla-rpc-port=2000
```

如果在服务器上无显示运行，可使用无渲染模式或虚拟显示；如果需要观察测试过程，在命令中增加：

```bash
--render
```

批量测试建议默认不开渲染，否则速度会明显变慢。

## 迁移到其他主车方法时的最小改动

推荐保留本项目的环境和评估脚本，只替换主车策略 adapter：

1. 保留 `envs/`、`training/train.py`、`scripts/evaluate_finetuned_ego_against_adv.py`、`scripts/run_ego_natural_seed_eval.py`。
2. 在测试脚本里把 `EgoPPOAdapter.from_config(...)` 和 `ego.load(...)` 替换为自己的主车策略加载逻辑。
3. 保证每个 step 输出一个 0 到 4 的离散动作。
4. 不修改对抗车 MAPPO 加载逻辑，这样可以继续使用上述 10 个冻结对抗车模型。
5. 保留日志字段和绘图逻辑，方便不同方法直接对比。

这样可以最大程度复用我们的场景、初始化随机化、对抗车模型和指标统计。
